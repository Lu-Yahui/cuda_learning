#include <chrono>
#include <cmath>
#include <iostream>

/**
 * @brief Simple version for square matrix mul
 *
 * @param M
 * @param N
 * @param P
 * @param width
 */
void SimpleMatrixMulGpu(float* M, float* N, float* P, int width);
